This is a libpwquality library for password quality checks
and generation of a random passwords that pass the checks.

NOTES:

This library uses the cracklib as a dependency.

See the pwquality.h header file for the API.
